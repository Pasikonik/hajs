import { getProperBill } from 'frontend/helpers/get-proper-bill';
import { module, test } from 'qunit';

module('Unit | Helper | get proper bill');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getProperBill([42]);
  assert.ok(result);
});
