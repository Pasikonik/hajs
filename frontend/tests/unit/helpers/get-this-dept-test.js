import { getThisDept } from 'frontend/helpers/get-this-dept';
import { module, test } from 'qunit';

module('Unit | Helper | get this dept');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getThisDept([42]);
  assert.ok(result);
});
