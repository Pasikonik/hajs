import { getProperRent } from 'frontend/helpers/get-proper-rent';
import { module, test } from 'qunit';

module('Unit | Helper | get proper rent');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getProperRent([42]);
  assert.ok(result);
});
