import { currentMonth } from 'frontend/helpers/current-month';
import { module, test } from 'qunit';

module('Unit | Helper | current month');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = currentMonth([42]);
  assert.ok(result);
});
